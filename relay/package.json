{
  "name": "marketplace-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker + Durable Object relay routing claude.ai MCP requests to each friend's Mac by identity.",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
